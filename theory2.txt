q1. UnderstandingPython’s PEP 8 guidelines.

PEP 8 (Python Enhancement Proposal 8) is the official style guide for Python code. It provides conventions for writing Python code that is readable, consistent, and maintainable across projects. Following PEP 8 ensures that Python code looks familiar to all Python developers.
Here’s a summary of the main PEP 8 guidelines, organized by category:

🧱 1. Code Layout
Indentation


Use 4 spaces per indentation level (no tabs).


Continuation lines should align naturally or be indented with 4 spaces.


def my_function(
    arg1, arg2, arg3
):
    return arg1 + arg2 + arg3

Maximum Line Length


Limit lines to 79 characters (72 for docstrings/comments).


Blank Lines


Use 2 blank lines before top-level function and class definitions.


Use 1 blank line between methods in a class.


Imports


Imports should be on separate lines, grouped in this order:


Standard library imports


Third-party imports


Local application imports




Use absolute imports whenever possible.


import os
import sys

import requests

from mypackage import mymodule


✍️ 2. Naming Conventions
General Guidelines


Use descriptive and consistent names.


Avoid single-letter names, except for counters or iterators (i, j, k).


Common Naming Styles
TypeConventionExampleVariable / functionlowercase_with_underscoresget_user_data()ClassCapWords (PascalCase)UserProfileConstantALL_CAPS_WITH_UNDERSCORESMAX_RETRIESPrivate member_single_leading_underscore_cache“Magic” methods__double_leading_and_trailing____init__

📄 3. Comments and Docstrings
Comments


Keep comments up to date with code.


Write full sentences, starting with a capital letter.


Use inline comments sparingly, separated by at least two spaces.


x = x + 1  # Increment counter

Docstrings


Use triple quotes (""" """) for docstrings.


Describe what the function/class/module does, not how.


def add(x, y):
    """Return the sum of two numbers."""
    return x + y


🧮 4. Whitespace in Expressions and Statements
Avoid unnecessary whitespace:
# Good:
spam(ham[1], {eggs: 2})

# Bad:
spam( ham[ 1 ], { eggs : 2 } )

Use spaces around operators and after commas:
x = 1 + 2
my_list = [1, 2, 3]

No extra spaces before parentheses or colons in function definitions:
def foo(bar):  # ✅ Good
def foo (bar):  # ❌ Bad


🧠 5. Programming Recommendations


Use is/is not when comparing to None:
if foo is None:
    ...



Don’t compare boolean values to True or False explicitly:
if is_valid:  # ✅
if is_valid == True:  # ❌



Use startswith() and endswith() instead of string slicing.



🧰 6. Tools That Help Enforce PEP 8
You can use tools to check your code automatically:


flake8 — checks style and linting errors.


black — automatically formats code to follow PEP 8.


pylint — provides more detailed style and code-quality feedback.


_____________________________________________________________________________________________________________________________________________

q2. Indentation, comments, and naming conventions in Python.
🧱 1. Indentation
Indentation in Python isn’t just about readability — it defines code blocks.
PEP 8 sets clear rules for consistent indentation.
✅ Guidelines:


Use 4 spaces per indentation level (not tabs).


Continuation lines (if a statement is too long) should either:


Align with the opening delimiter, or


Be indented by an extra 4 spaces.




💡 Examples:
# Correct indentation (4 spaces)
def greet(name):
    print("Hello,", name)
    print("Welcome!")

# Continuation lines aligned properly
total = (first_number + second_number +
         third_number + fourth_number)

🚫 Avoid mixing tabs and spaces — it causes IndentationError.

💬 2. Comments
Comments explain why code does something, not what it does.
They should be clear, concise, and updated when code changes.
✅ Guidelines:
Inline comments


Begin with #, preceded by at least two spaces.


Keep them short and meaningful.


x = x + 1  # Increment counter

Block comments


Apply to a block of code; each line starts with #.


Leave a blank line before the comment block.


# Process user input
# and validate it before saving
user_input = input("Enter your name: ")

Docstrings


Describe modules, functions, classes, and methods.


Use triple quotes (""" """).


def add(a, b):
    """Return the sum of a and b."""
    return a + b


🏷️ 3. Naming Conventions
Consistent naming makes code easier to understand and maintain.
✅ PEP 8 Naming Rules:
TypeConventionExampleVariableslower_case_with_underscoresuser_name, max_speedFunctionslower_case_with_underscoresget_data(), send_email()ClassesCapWords (PascalCase)UserProfile, BankAccountConstantsALL_CAPS_WITH_UNDERSCORESMAX_LIMIT, DEFAULT_PORTPrivate members_single_leading_underscore_cache, _helper()Special methods__double_leading_and_trailing____init__, __str__

⚠️ Tips


Choose meaningful names: total_sales is better than ts.


Avoid single-letter names, except for loop counters (i, j, k).


Be consistent — don’t mix naming styles in the same project.



🔍 Quick Summary
CategoryRuleExampleIndentation4 spaces per leveldef func(): → (indent 4 spaces)Inline Comment2 spaces before #x = 10  # count starts hereDocstringTriple quotes"""Describe function here."""Variable Namesnake_caseuser_nameClass NamePascalCaseBankAccount
_____________________________________________________________________________________________________________________________________________


q3. Writingreadable and maintainable code.

🧠 What “Readable & Maintainable” Means

Readable code is:

Easy to understand at a glance.

Clearly structured and well-formatted.

Uses descriptive names and meaningful comments.

Maintainable code is:

Easy to update or extend without breaking things.

Modular (broken into reusable pieces).

Tested and consistent with style conventions.

🧱 1. Follow Consistent Code Style (PEP 8)

PEP 8 is Python’s style guide — following it ensures consistency.

✅ Key practices:

Use 4 spaces for indentation.

Limit lines to 79 characters.

Use blank lines to separate functions and classes.

Add whitespace around operators for clarity.

# Good
total_price = item_price + tax

# Bad
total_price=item_price+tax

🏷️ 2. Use Descriptive Names

Choose names that explain what the variable or function does, not how it does it.

✅ Good:

def calculate_total_price(items):
    ...


❌ Bad:

def calc(x):
    ...


Naming rules:

Variables/functions → snake_case

Classes → PascalCase

Constants → ALL_CAPS

🧩 3. Keep Code Modular and Reusable

Break large code blocks into small, focused functions that do one thing well.

✅ Good:

def get_user_input():
    return input("Enter your name: ")

def greet_user(name):
    print(f"Hello, {name}!")

def main():
    name = get_user_input()
    greet_user(name)


❌ Bad (all in one block):

name = input("Enter your name: ")
print(f"Hello, {name}!")

💬 4. Write Clear Comments and Docstrings

Comments explain why code exists, not what it’s doing — the code itself should already make the “what” clear.

✅ Good:

def divide(a, b):
    """Return division result or None if division by zero."""
    if b == 0:
        return None  # Prevent ZeroDivisionError
    return a / b


❌ Bad:

def divide(a, b):
    # divide a by b
    return a / b

🧠 5. Follow the DRY Principle (Don’t Repeat Yourself)

Avoid code duplication — put repeated logic into reusable functions or classes.

✅ Good:

def calculate_discount(price, discount):
    return price - (price * discount)


❌ Bad:

price1 = price1 - (price1 * discount)
price2 = price2 - (price2 * discount)

🧰 6. Handle Errors Gracefully

Use try-except blocks thoughtfully and log or raise meaningful errors.

try:
    result = 10 / user_input
except ZeroDivisionError:
    print("You can’t divide by zero.")

🧪 7. Test and Document Your Code

Write unit tests to ensure each function works as expected.

Keep docstrings up to date.

Include usage examples when possible.

def add(a, b):
    """Return the sum of two numbers.
    
    Example:
        >>> add(2, 3)
        5
    """
    return a + b

🧹 8. Use Tools to Maintain Quality

Automate style checking and formatting:

black — auto-formats code.

flake8 — checks PEP 8 compliance.

pylint — checks for bugs and poor practices.

🔍 Summary: Best Practices for Readable, Maintainable Code
Principle	Why It Matters	Example
Follow PEP 8	Consistent and clean layout	4-space indent, clear spacing
Use meaningful names	Self-explanatory code	user_count instead of uc
Comment wisely	Explains “why”	# prevent division by zero
Write small functions	Easier testing and debugging	def process_data()
Avoid repetition	Easier updates	Reuse logic via functions
Handle errors	Prevents crashes	try/except
Document & test	Future-proof code	"""Docstring""" and tests

_____________________________________________________________________________________________________________________________________________


